/**
 * Legacy placeholder generator — DISABLED.
 * Real green "nt" favicons were installed from God's attachments (see public/icons/README-favicons.txt).
 * Re-running this script would overwrite them with black "NT" placeholders.
 * To refresh icons, copy from attachments and resize 32×32 from the 512 source with sharp.
 */
console.error("generate-favicons.mjs: refused — real Dibya favicons are installed. See public/icons/README-favicons.txt");
process.exit(1);
