{
  "name": "notaxotcalc",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test:golden": "node --experimental-strip-types tests/golden.mjs",
    "pages:deploy": "npm run build && wrangler pages deploy dist --project-name=notaxotcalc",
    "favicons": "node scripts/generate-favicons.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.3.3",
    "tailwindcss": "^4.3.3"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "sharp": "^0.35.4",
    "wrangler": "^4.135.0"
  },
  "description": "NoTaxOTCalculator — Schedule 1-A No Tax on Overtime / Tips income-tax deduction estimator"
}
